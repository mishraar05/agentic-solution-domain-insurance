# Rules Change Log

Rules evolve while we build. Each change gets one dated line: what changed, why, decided by whom. This is the audit trail that replaces heavyweight change control during the PoC; it carries forward into the customer environment.

| Date | Rule changed | New position | Why | Decided by |
|---|---|---|---|---|
| 2026-07-13 | "No LLM before Source Intelligence gate passes" | LLM permitted for one bounded capability: Source Documentation Agent over prompt-eligible structural context only. Vector search, knowledge retrieval, auto-approval, and LLM-derived confidence remain prohibited. | Documentation is the lowest-risk, highest-learning first LLM use; exercises the full agent harness early. | Sponsor (ratified in chat, 2026-07-13) |
| 2026-07-13 | Platform-neutral design | Workspace quotas are runtime and operational concerns only; designs, contracts, and documentation remain platform-complete. | The PoC will scale into a customer environment, so workspace-specific architecture would create rework. | Sponsor |
| 2026-07-13 | Profiling evidence sources | Lakehouse Monitoring added as a second governed profile-evidence producer via `lakehouse_monitoring_adapter.py`: privacy gate applied post-computation, k-anonymity rule (cardinality <= 20, value count >= 5) for value summaries, LAKEHOUSE_MONITORING producer tag in minimization_rule. Custom governed queries remain the primary producer. | Platform-native profiling wanted for scale-up; adapter keeps evidence policy enforcement in our code. | Sponsor |
| 2026-07-13 | Semantic inference source | Embedded Python rules (EXACT_MATCHES, PII tokens, type keywords) superseded by versioned rule packs (`knowledge_packs/rule_packs/`) consumed by `knowledge_classifier.py`. Every inference records rule_pack_id, rule_version, rule_id. Selection metadata (source_system, naming convention, effective date) supports per-source packs. Workflow 02 and evaluation now use the pack API; the hardcoded modules are retired. | Codex + Claude converged: workflow framework scales, embedded inference knowledge does not (AS400 test: POLNBR/CLMSTS now resolve). | Sponsor |
| 2026-07-13 | Rule-pack resolution and abbreviation ambiguity | Select packs deterministically by exact source system, naming convention, priority, and effective date; equal-precedence ties fail closed. Duplicate token expansions remain multiple hypotheses. Ambiguity leaves ontology meaning unresolved, marks naming evidence `CONTRADICTED`, records the closed candidate set in the contradiction, and requires semantic-mapper or steward review. | Multi-source scale requires deterministic pack precedence; abbreviations such as EXP cannot be resolved safely by YAML order. | Sponsor |
| 2026-07-13 | `documentation_model_endpoint` default | Set to placeholder `REPLACE_WITH_CONFIRMED_MODEL_ENDPOINT` instead of a named model; DAG and job structure unchanged. | Model endpoint availability varies by workspace, so configuration must be supplied by the deployment environment. | Sponsor |
| 2026-07-13 | Confirmed documentation endpoint | Replaced the placeholder with workspace endpoint `databricks-gpt-5-6-luna`, observed `READY` for `llm/v1/chat`; invocation evidence remains pending. | Read-only endpoint discovery found a current cost-efficient hosted chat endpoint, allowing configuration without inventing a model name. | Sponsor |
| 2026-07-13 | Batch-compatible documentation endpoint | Replaced `databricks-gpt-5-6-luna` with `databricks-gpt-oss-20b`. The former is real-time only in this workspace and failed `ai_query` session creation; the latter is READY, batch-supported, and documented for structured `responseFormat`. | A serving endpoint being READY does not imply AI Functions batch support; documentation generation uses `ai_query`, so batch compatibility is mandatory. | Sponsor |
| 2026-07-13 | Inconsistent documentation model output | Structurally valid outputs that violate `PROPOSED`/`UNRESOLVED` field semantics are deterministically downgraded to `UNRESOLVED`; proposal fields are discarded and the inconsistency is recorded as a contradiction for Domain Steward review. | A single contradictory model row must not fail the governed batch or persist invented semantics. | Sponsor and Codex |
| 2026-07-13 | Source Intelligence release gate | Runtime validation, documentation quality, independent labels, confidence settings, and governance roles approved under non-PII reviewer identifier `HUMAN_REVIEWER_01`; Source Intelligence marked PASSED and Governed Knowledge Foundation unlocked. | Human reviewer explicitly confirmed all approvals and authorized Governed Knowledge Foundation. | Sponsor (`HUMAN_REVIEWER_01`) |
| 2026-07-13 | Synthetic COTS baseline | Initially proposed Policy Admin as the first vendor-neutral synthetic baseline; this was superseded the same day by separate Policy Admin, Claims Management, and Billing module packs. Real-vendor identification remains prohibited without an exact authorized licensed pack. | Proves versioned recognition and extension handling without committing proprietary material or overclaiming Guidewire/Majesco/Origami knowledge. | Codex implementation proposal; expanded by Sponsor direction |
| 2026-07-13 | Governed Knowledge Foundation review sequencing | Intermediate human review no longer blocks technical work packages. Build and validate the end-to-end solution first; keep all recommendations `PROPOSED`, then design the consolidated review workflow and perform authoritative approval. | The current per-package approval pattern was judged premature and not useful before reviewers can see the complete solution. | Sponsor (directed in chat) |
| 2026-07-13 | Capability naming and environment language | Numeric stage labels and edition-specific workspace references were replaced with stable capability names and platform-neutral wording across plans, evidence, skills, code, tests, and generators. | Capability names explain purpose without coupling the solution to an implementation sequence or workspace edition. | Sponsor (directed in chat) |
| 2026-07-13 | Skill responsibility naming | Router skills must use responsibility-specific names; the work-package router is named `governed-work-package-router` consistently in its folder, trigger, UI metadata, and planning references. | The skill name must state that it routes governed work packages instead of presenting a vague next-step action. | Sponsor (directed in chat) |
| 2026-07-13 | Scalable solution governance | Replaced the pilot-only governance skill with `establish-insurance-solution-governance`; shared controls are domain-neutral, Policy and Claims use separate references, new domains use a governed template, and operational readiness is an explicit reference. | Governance must scale by domain and operating scope without an inception stage or a monolithic skill. | Sponsor (directed in chat) |
| 2026-07-13 | Knowledge-pack immutability | Preserved `cots_like_reference / 1.0.0` as immutable authoring evidence and registered the exact-scope derived pack as the distinct identity `cots_like_policy_admin / 1.0.0` with its own fingerprint. | Repointing or rewriting an existing pack identity would destroy provenance and contradict immutable-version governance. | Codex implementation decision under approved pack rules |
| 2026-07-13 | Synthetic COTS module coverage | Expanded the development baseline from Policy Admin only to separate Policy Admin, Claims Management, and Billing packs; ontology normalization now selects declared domains instead of hardcoded concept IDs, and inference contexts remain module-scope isolated. | A scalable COTS reference must cover the suite's governed modules without mixing product/module/version provenance. | Sponsor (directed in chat) |
| 2026-07-13 | Canonical ontology representation | `knowledge_packs/ontology/pnc_ontology_v1.yaml` is the single authoring source for the P&C ontology; parallel hand-maintained JSON is prohibited and any future compatibility representation must be generated from YAML with drift validation. | One canonical, human-readable representation prevents semantic and version drift across duplicate ontology files. | Sponsor (directed in chat) |
| 2026-07-13 | Federated ontology authoring | The canonical ontology is a federated YAML bundle: one root descriptor plus separate Policy, Claims, Billing, and Shared concept modules. The ordered bundle has one fingerprint and fails closed on missing, duplicate, cross-domain, path-escaping, or cross-version modules. | Domain-sized files are readable and independently maintainable without weakening pack identity or provenance. | Sponsor (directed in chat) |
| 2026-07-14 | Insurance ontology product-area coverage | Added the distinct `insurance_ontology / 1.0.0` federated bundle with preserved P&C concept IDs plus Risk, Document, Motor, Home, Commercial Property, and isolated Pensions modules. Pensions remains outside the P&C taxonomy; all content is independently authored synthetic knowledge. | The governed knowledge foundation must cover the four requested product areas without mixing retirement and P&C semantics or weakening provenance. | Sponsor (directed in chat) |
| 2026-07-14 | Retire the pnc_ontology pack | Deleted `knowledge_packs/ontology/pnc_ontology_v1.yaml` and `ontology/modules/`; `insurance_ontology / 1.0.0` is now the sole canonical ontology pack and authoring tree, superseding the 2026-07-13 "single authoring source" entry above. All 64 legacy `pnc_ontology / 1.1.0` concept IDs remain verified present in the broader ontology via a frozen regression check. | Two parallel ontology trees with duplicate-named module files were confusing to navigate and no longer served a purpose once the broader ontology carried every legacy ID forward. | Sponsor (directed in chat) |
